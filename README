NAMI - Ncurses Asterisk Manager Interface
-----------------------------------------

libami v0.01 pre-alpha

libami is a C library for interfacing the asterisk manager 

nami is the ncurses portion



devels packages to build
* db4-devel
* curses-devel


On ubuntu
   sudo apt-get install libdb4.8-dev
   sudo apt-get install libncurses5-dev
   sudo apt-get install binutils-dev

